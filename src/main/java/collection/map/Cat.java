package collection.map;

public class Cat {
    String name;

    public Cat(String s) {
        this.name = name;
    }

    @Override
    public String toString() {
        return name != null ? name.toUpperCase() : null;
    }
}
